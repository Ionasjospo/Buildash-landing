# Buildash Landing

Landing page oficial (MVP) para **Buildash**.  
Proyecto **solo frontend** (React + TypeScript) construido con **Vite** y estilado con **Tailwind CSS v3**.

> ⚠️ Nota: esto NO es el sistema Buildash. Es únicamente la landing pública/marketing.

---

## 🧱 Stack

- **Node.js** (tooling) + **npm**
- **Vite** (dev server + build)
- **React + TypeScript**
- **Tailwind CSS v3** + PostCSS + Autoprefixer

---

## ✅ Requisitos

- Node **18+** (recomendado 20)
- npm **9+**

Verificar:
```bash
node -v
npm -v
